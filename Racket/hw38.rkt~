; hw#38
; Yedoh Kang <kang.yedoh@gmail.com> period06
; time started
; time ended

(display "#1 introduction") (newline)

(cdr (cdr (list 0 1 2 3)))
; The list function builds a list from its parameters
; in memory. Therefore
;     (list 0 1 2 3)
; produces a list that matches the list literal
;     '(0 1 2 3)
;
; The cdr function produces a list that is a copy of
; its list argument without that list's first element.
; Therefore
;     (cdr '(0 1 2 3))
; produces a list that matches the list literal
;     '(1 2 3)
;
; The cdr function produces a list that is a copy of
; its list argument without that lists's first element.
; Therefore the outermost call
;      (cdr '(1 2 3))
; produces a list that matches the list literal
;      '(2 3)

(define M '(a (b c) d ((e f) g h) i))